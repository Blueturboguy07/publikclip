name: bugfix-lab

on:
  workflow_dispatch:

jobs:
  oracle:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run oracle body
        shell: powershell
        run: ./scripts/bugfix-lab/publikclip-windows-git-not-found.ps1
